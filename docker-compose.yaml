services:
  web:
    build: .
    privileged: true
    ports:
      - 8088:80
      - 8807:8080
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - dpanel
networks:
  dpanel:
    name: dpanel-local
